# 漫味 (ManWei) 项目测试报告 — 05 回归测试

> 本文档记录 BUG-001 数据清理后的回归验证。**禁止笼统的"回归测试通过"**——每步写明请求/响应。

---

## 1. 概述

| 字段 | 内容 |
|---|---|
| **回归触发原因** | BUG-001 数据清理：DELETE /api/EmotionTags/49 与 /52 |
| **回归目标** | 验证清理后无脏数据残留、未引入新问题 |
| **执行日期** | 2026-06-20 |
| **测试账号** | `<TEST_USER>`（密码已脱敏） |
| **后端地址** | `http://localhost:5150` |

---

## 2. 回归范围

| 项目 | 是否纳入 | 原因 |
|---|---|---|
| BUG-001 清理效果验证（EmotionTags） | ✅ | 直接受影响 |
| 收藏列表（Favorites） | ✅ 抽查 | 同一测试账号数据，验证无副作用 |
| 情绪曲线（EmotionCurves） | ✅ 抽查 | 同上 |
| 推荐接口 | ❌ | 不受标签清理影响（理论无关） |
| AI 对话 | ❌ | 同上 |
| 全量 12 用例 | ❌ | 受限于时间窗，未做全量回归 |

---

## 3. 回归测试执行记录

### R1：删除脏数据 Id=49

| 字段 | 内容 |
|---|---|
| **请求** | `DELETE http://localhost:5150/api/EmotionTags/49` |
| **Header** | `Authorization: Bearer {{token}}` |
| **响应状态码** | 200 |
| **响应体** | `删除成功` |

### R2：删除脏数据 Id=52

| 字段 | 内容 |
|---|---|
| **请求** | `DELETE http://localhost:5150/api/EmotionTags/52` |
| **Header** | `Authorization: Bearer {{token}}` |
| **响应状态码** | 200 |
| **响应体** | `删除成功` |

### R3（关键）：验证已使用标签列表无脏数据

| 字段 | 内容 |
|---|---|
| **请求** | `GET http://localhost:5150/api/EmotionTags/used` |
| **Header** | `Authorization: Bearer {{token}}` |
| **响应状态码** | 200 |
| **响应体** | `["ASCII_TAG","冲击","催泪","剧情向","热血","热血少年","神作","思考","治愈"]` |
| **验证结论** | **仅 9 条正常标签，无 Id=49(`?`) 或 Id=52(`ȼ`) 的任何残留** |

### R4：验证清理操作未影响收藏数据

| 字段 | 内容 |
|---|---|
| **请求** | `GET http://localhost:5150/api/Favorites?Page=1&PageSize=10` |
| **Header** | `Authorization: Bearer {{token}}` |
| **响应状态码** | 200 |
| **响应体** | 列表仍含之前创建的 favoriteId=170（status=2, rating=9, progress=25） |
| **验证结论** | 收藏数据未受标签清理操作影响 |

### R5：验证清理操作未影响情绪曲线

| 字段 | 内容 |
|---|---|
| **请求** | `GET http://localhost:5150/api/EmotionCurves/170` |
| **Header** | `Authorization: Bearer {{token}}` |
| **响应状态码** | 200 |
| **响应体** | 仍含 5 条情绪记录（ep=1/5/12/20/25，emo=5/4/3/5/4） |
| **验证结论** | 情绪曲线数据未受标签清理操作影响 |

---

## 4. 回归结论

### 4.1 BUG-001 修复

- ✅ 清理**已生效**：Id=49 与 Id=52 已从数据库删除
- ✅ 无任何脏数据残留：`GET /api/EmotionTags/used` 返回 9 条正常标签
- ✅ 未误删正常数据：清理前 9 条正常标签全部保留

### 4.2 无新问题引入

- ✅ 收藏模块（R4）：favoriteId=170 数据完整，未受影响
- ✅ 情绪曲线（R5）：5 条记录完整，未受影响
- ✅ 截图证据：`screenshots/optimization-bug-001-after-cleanup.png`

### 4.3 回归范围限制说明

- 本次回归仅针对 BUG-001 数据清理的**副作用检查**，非全量 12 用例回归
- R4/R5 仅**抽查** 1 个 favoriteId（170），未覆盖所有用户数据
- 推荐接口与 AI 接口在清理后**未再单独测试**（理论上不受标签表操作影响，但本次未实际验证）
- 如需全面回归，参考 `01-test-design.md` 的 12 用例表逐条执行
